{
  "scripts": {
    "build": "node scripts/write-public-config.mjs && tailwindcss -i ./src/styles.css -o ./assets/css/styles.css --minify",
    "check": "npm run build"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
